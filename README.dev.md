# Постановка задачи

Автоматический поиск архитектуры под простую задачу с помощью LLM - написание агента.

Разработка автоматического поиска архитектур на основе LLM для задачи форкастинга, TIME-Series, метрика MSE. Датасеты
ETH1, ETH2 (https://github.com/zhouhaoyi/ETDataset).

Что ожидается от проекта:

- Цикл обучения одной модели можно взять модель Informer (https://github.com/zhouhaoyi/Informer2020). Цикл обучения
  выдает финальую метрку и включает перебор параметров.
- Метод оценки и генрации архитектур через LLM. LLM принимает на вход архитектуру модели и текущее качество, выдает
  новую архитектуру модели.

Для это задачи не нужно обучать LLM, можно взять обученную или вызывать через API. Цель проекта собрать пайплайн с
обратной связью, где модель бы предлагала лучшую архитектуру. Сравнить результат с оптимизаций гиперпепараметров через
OPTUNA, где изменяемые параметры это размер вектора, число слоев и т.д.

# 04.11.2025

# Статус

Что сделано:

1) Промпт для генерации файлов обучения модели
2) Пайплайн интеграции с моделью через OpenAI API, отправляет промпт генерации кода и парсит из ответа LLM файлы для
   запуска обучения

Что нужно еще сделать до прототипа:

3) Раннер решения, собирающий метрики качества модели для дальнейшей оптимизации
4) Сэмплер, который бы выдавал top_k решений для их комбинирования
5) Собственно промпты для того чтобы модель брала два решения и выдавало новое

Архитектурно заложено, что LLM выдает три файла - model.py, training_args.json, model_config.json.
Это удобно, т.к:

- Можно комбинировать модели одной архитектуры, с разными гиперпараметрами (exploitation)
- Можно комбинировать модели совершенно разных архитектур (exploration)
- Это решает проблему о которой преподы писали в беседу, что нужно перебирать и вернеуровнево архитектуру, и
  гиперпараметры

# Информация от менторов

```text
да, но при сравнении хочется опять же оценить не только качество, но и бюджет на обучение

причем можно сделать три категории

1. LLM предсказывает гиперпараметры
2. оптуна предсказывает гиперпаоаметры

смотрим что из этого лучше

3. LLM предсказывает архитектуру, смотрим лучше ли это чем предасказание гиперпараметров
```

```text
1. хочется понять что работает лучше, optuna или LLM.

2. хочется что-то рабочее, что бы можно потом это воспроизвести и провалидировать
```

```text
Хотим сделать такое.

1. Перебор параметров с оптуной.

2. Перебор параметров с агентом.

3. Перебор параметров и архитектуры с агентом.

В итоге мы сравним 1,2,3 между собой и посмотрим что работает лучше.
```

```text

```

```text

```

```text

```

```text

```

# 08.11.2025

# Статус

Запушил, в main.py я запускал прототип, там очень примерное взаимодействие частей системы. Ну в конечном решении там
должен быть полноценный цикл с сэмплированием успешных кандидатов и скрещиванием.

объект класса UnsafeRunner нужно создавать новый на каждый отдельный run, потом починю.

если решение-кандидат выдает исключение, оно нормально распространяется вверх, его можно поймать (обернуть в try-except
метод run раннера) и дать кандидату плохую награду или типа того.

для решений-кандидатов я замерял две метрики.

AUC лосса по времени, он хорошо отображает и качество и скорость обучения модели
и энергопотребление видеокарты.

искать мы должны по идее оптимальные по парето.

энергопотребление чет криво сделал, не захватывает значение, потом пофикшу, можешь пока запихнуть заглушку-значение для
теста.